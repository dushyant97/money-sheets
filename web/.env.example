# Offline mode — no Google Cloud or OAuth required.
# Data is stored on the device (browser localStorage / mobile AsyncStorage).
#
# Optional Turso (libSQL) sync is configured in the app UI under
# "Budgets & Data > Storage", not via environment variables. Your Turso URL and
# auth token are saved only on your device (localStorage).
